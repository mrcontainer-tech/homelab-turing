# CloudNativePG Operator values
# The operator manages PostgreSQL Cluster CRDs across all namespaces

replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 256Mi
