# Use this file to override default values.yaml with custom values.yaml
csi:
  kubeletRootDir: /var/lib/kubelet

metrics:
  serviceMonitor:
    enabled: true
    interval: 30s
    additionalLabels:
      release: kube-prometheus-stack-chart

persistence:
  defaultClassReplicaCount: 1

