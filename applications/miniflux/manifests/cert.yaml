apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: miniflux-tls
  namespace: miniflux
spec:
  secretName: miniflux-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - miniflux.mrcontainer.nl
