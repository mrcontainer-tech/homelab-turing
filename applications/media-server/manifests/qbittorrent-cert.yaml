apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: qbittorrent
  namespace: media
spec:
  secretName: qbittorrent-tls
  dnsNames:
    - qb.mrcontainer.nl
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer